{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "graphql-request": "^3.0.0",
    "jest": "^26.2.2"
  },
  "scripts": {
    "test": "jest --reporters=default --reporters=./jest-reporter/jest-reporter.js --verbose"
  }
}
